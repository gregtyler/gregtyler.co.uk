---
layout: base.njk
eleventyExcludeFromCollections: true
---
<div class="c-splash">
  Code with a conscience
</div>

<div class="c-sunken">
  <h2 class="c-sunken__title">Recent blog posts</h2>
  <div class="o-carousel">
    {% set latest = collections.all.reverse() %}
    {% for post in latest.slice(0, 5) %}
      <a href="{{ post.url }}" class="c-card c-card--link">
        <h2 class="c-card__title">{{ post.data.title }}</h2>
        <div class="c-card__meta">{{ post.date | date }}</div>

        <div class="t-flex--fill"></div>
        <div class="c-card__actions">
          <span class="c-card__action c-card__action--primary">
            Read post
          </span>
        </div>
      </a>
    {% endfor %}
  </div>
</div>
